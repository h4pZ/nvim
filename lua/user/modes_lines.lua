require('modes').setup({
  colors = {
    copy = "#f5c359",
    delete = "#f70067",
    insert = "#aeb1ff",
    visual = "#8cd7ff",
  },

  -- Cursorline highlight opacity
  line_opacity = 0.2,

  -- Highlight cursor
  set_cursor = true,

  -- Highlight in active window only
  focus_only = true
})

